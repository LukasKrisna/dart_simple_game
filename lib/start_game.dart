class StartGame {
  late String loading;

  StartGame({String loading = 'Start a new game..'}) {
    // loading = "Start a new game..";
    // ignore: prefer_initializing_formals
    this.loading = loading;
  }
}
